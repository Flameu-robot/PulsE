name: Feed Service CI

on:
  push:
    branches: [ "dev", "feature/feed-service" ]
    paths:
      - 'backend/feed-service/**'
      - 'backend/shared/**'
      - 'backend/config/**'
      - 'backend/gradle/**'
      - 'backend/build.gradle.kts'
      - 'backend/settings.gradle.kts'

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  build-and-lint:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'
          cache-dependency-path: backend/gradle/libs.versions.toml

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run Checkstyle
        run: ./gradlew :feed-service:checkstyleMain
        continue-on-error: true

      - name: Publish Checkstyle Report
        uses: jwgmeligmeyling/checkstyle-github-action@v1
        with:
          path: 'backend/feed-service/build/reports/checkstyle/main.xml'
          token: ${{ secrets.GITHUB_TOKEN }}
          name: Checkstyle Annotations (Feed Service)

      - name: Build project
        run: ./gradlew :feed-service:build -x test

      - name: Run tests
        run: ./gradlew :feed-service:test
        env:
          SPRING_PROFILES_ACTIVE: test

      - name: Upload Reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: reports-feed-service
          path: |
            backend/feed-service/build/reports/tests/
            backend/feed-service/build/reports/checkstyle/