```
Браузер                         Сервер                      Аутентификатор
   │                               │                            (телефон/USB)
   │  1. POST /register/start      │
   │  ─────────────────────────►   │
   │                               │ Генерирует challenge
   │                               │ (случайные байты)
   │  ◄─────────────────────────   │
   │  challenge + rpId + userId    │
   │                               │
   │  navigator.credentials.create()
   │  ─────────────────────────────────────────────────────►
   │                                                        Пользователь
   │                                                        подтверждает
   │                                                        (палец/PIN/кнопка)
   │                                                        
   │                                                        Генерирует пару
   │                                                        ключей (priv+pub)
   │                                                        Подписывает challenge
   │                                                        приватным ключом
   │  ◄─────────────────────────────────────────────────────
   │  credentialId + publicKey + подпись
   │
   │  2. POST /register/finish     │
   │  ─────────────────────────►   │
   │                               │ Проверяет подпись
   │                               │ Сохраняет publicKey + credentialId
   │  ◄─────────────────────────   │
   │  OK                           │
```
```
Браузер                         Сервер                      Аутентификатор
   │                               │
   │  1. POST /login/start         │
   │  ─────────────────────────►   │
   │                               │ Генерирует challenge
   │  ◄─────────────────────────   │
   │  challenge + allowCredentials │
   │                               │
   │  navigator.credentials.get()  │
   │  ─────────────────────────────────────────────────────►
   │                                                        Находит ключ для
   │                                                        этого rpId
   │                                                        Подписывает challenge
   │                                                        приватным ключом
   │  ◄─────────────────────────────────────────────────────
   │  credentialId + подпись + signCount
   │
   │  2. POST /login/finish        │
   │  ─────────────────────────►   │
   │                               │ Достаёт publicKey из БД
   │                               │ Проверяет подпись
   │                               │ Проверяет signCount
   │  ◄─────────────────────────   │
   │  JWT tokens                   │
```
